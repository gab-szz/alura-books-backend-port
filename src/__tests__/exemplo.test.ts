// Exemplo de estrutura de teste
// Remova este arquivo quando criar seus próprios testes

import { describe, it, expect } from "@jest/globals";

describe("Exemplo de teste", () => {
  it("deve passar", () => {
    expect(1 + 1).toBe(2);
  });
});
